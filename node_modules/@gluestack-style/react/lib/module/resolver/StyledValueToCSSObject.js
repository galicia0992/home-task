import { resolvedTokenization } from '../utils';
export function StyledValueToCSSObject(input, CONFIG, ignoreKeys = new Set()) {
  if (!input) {
    return {};
  }
  return resolvedTokenization(input, CONFIG, ignoreKeys);
}
//# sourceMappingURL=StyledValueToCSSObject.js.map